# Mock report profiles for local demos.
default:
  owner: example-org
  repo: example-repo
  title: QA Daily Report
  output_dir: reports
  timezone: Asia/Seoul
  scenario: normal

release-risk:
  owner: example-org
  repo: checkout-service
  title: QA Release Risk Report
  output_dir: release-risk-reports
  timezone: Asia/Seoul
  scenario: release-risk

quiet:
  owner: example-org
  repo: docs-portal
  title: QA Quiet Day Report
  output_dir: quiet-day-reports
  timezone: Asia/Seoul
  scenario: quiet
